{
  "name": "02 · Calls — Drive poll, transcribe & evaluate",
  "nodes": [
    {
      "parameters": {
        "rule": { "interval": [{ "field": "minutes", "minutesInterval": 15 }] }
      },
      "id": "sched",
      "name": "Every 15 min",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [-760, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT coalesce(max(finished_at), now() - interval '2 days') AS since\nFROM job_runs\nWHERE job_name = 'calls_ingest' AND status = 'succeeded';",
        "options": {}
      },
      "id": "watermark",
      "name": "Read watermark",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [-540, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } },
      "notes": "Resume from the last SUCCESSFUL run, not from 'now'. A failed run must not silently skip the window it was meant to cover."
    },
    {
      "parameters": {
        "resource": "fileFolder",
        "queryString": "=",
        "filter": {
          "folderId": { "__rl": true, "value": "={{ $env.DRIVE_CALLS_FOLDER_ID }}", "mode": "id" },
          "whatToSearch": "files"
        },
        "options": { "fields": ["id", "name", "size", "modifiedTime", "mimeType"] }
      },
      "id": "list-files",
      "name": "List Drive recordings",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3,
      "position": [-320, 300],
      "credentials": {
        "googleApi": { "id": "drive-sa", "name": "Drive service account" }
      },
      "notes": "Service account, not user OAuth: a scheduled job cannot complete an interactive consent screen."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT NOT EXISTS (\n  SELECT 1 FROM interactions\n  WHERE external_source = 'asterisk_drive' AND external_id = $1\n) AS is_new;",
        "options": { "queryReplacement": "={{ $json.name.split('-').pop().replace('.wav','') }}" }
      },
      "id": "seen",
      "name": "Already ingested?",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [-100, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } }
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "version": 2 },
          "conditions": [
            {
              "leftValue": "={{ $json.is_new }}",
              "rightValue": true,
              "operator": { "type": "boolean", "operation": "true", "singleValue": true }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "if-new",
      "name": "New recording?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [120, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.WORKER_URL }}/calls/parse-name",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [{ "name": "filename", "value": "={{ $('List Drive recordings').item.json.name }}" }]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [{ "name": "X-API-Key", "value": "={{ $env.WORKER_API_KEY }}" }]
        },
        "options": { "timeout": 15000 }
      },
      "id": "parse-name",
      "name": "Decode PBX filename",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [340, 300],
      "notes": "q-3009-0500000000-20260701-170522-<uniqueid>.wav yields agent extension, caller number and start time. Also reports clock drift between the filename and the Asterisk uniqueid."
    },
    {
      "parameters": {
        "operation": "download",
        "fileId": { "__rl": true, "value": "={{ $('List Drive recordings').item.json.id }}", "mode": "id" },
        "options": { "binaryPropertyName": "audio" }
      },
      "id": "download",
      "name": "Download audio",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3,
      "position": [560, 300],
      "credentials": { "googleApi": { "id": "drive-sa", "name": "Drive service account" } }
    },
    {
      "parameters": {
        "operation": "toFile",
        "fileName": "=/tmp/customer360/{{ $('Decode PBX filename').item.json.uniqueid }}.wav",
        "dataPropertyName": "audio",
        "options": {}
      },
      "id": "to-disk",
      "name": "Write to shared volume",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [780, 300],
      "notes": "n8n and the worker share a Railway volume mounted at /tmp/customer360, so the audio is never base64'd through HTTP."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.WORKER_URL }}/calls/transcribe",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [{ "name": "X-API-Key", "value": "={{ $env.WORKER_API_KEY }}" }]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ audio_path: '/tmp/customer360/' + $('Decode PBX filename').item.json.uniqueid + '.wav' }) }}",
        "options": { "timeout": 900000 }
      },
      "id": "transcribe",
      "name": "Cohere Arabic ASR",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1000, 300],
      "notes": "15-minute timeout: an 8-minute call is ~13 chunks and the Space queues. Chunking happens inside the worker at silence boundaries."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH ins AS (\n  INSERT INTO interactions (\n    external_source, external_id, channel, started_at, duration_seconds,\n    customer_phone_raw, customer_phone_e164, agent_id, handled_by\n  ) VALUES (\n    'asterisk_drive', $1, 'phone_call'::channel, $2::timestamptz, $3::int,\n    $4, $5,\n    (SELECT agent_id FROM agents WHERE phone_extension = $6),\n    'agent'::speaker_role\n  )\n  ON CONFLICT (external_source, external_id) DO UPDATE SET updated_at = now()\n  RETURNING interaction_id\n)\nINSERT INTO transcripts (\n  interaction_id, audio_uri, duration_seconds, sample_rate_hz, channels,\n  asr_provider, asr_model_version, asr_confidence, language,\n  full_text, segments, diarization\n)\nSELECT interaction_id, $7, $3::numeric, $8::int, $9::int,\n       $10, $11, $12::numeric, 'ar', $13, $14::jsonb, $15\nFROM ins\nON CONFLICT (interaction_id) DO UPDATE SET\n  full_text = EXCLUDED.full_text,\n  segments = EXCLUDED.segments,\n  asr_confidence = EXCLUDED.asr_confidence\nRETURNING interaction_id;",
        "options": {
          "queryReplacement": "={{ $('Decode PBX filename').item.json.uniqueid }},{{ $('Decode PBX filename').item.json.started_at }},{{ Math.round($json.duration_seconds) }},{{ $('Decode PBX filename').item.json.customer_phone_raw }},{{ $('Decode PBX filename').item.json.customer_phone_e164 }},{{ $('Decode PBX filename').item.json.agent_extension }},drive://{{ $('List Drive recordings').item.json.id }},{{ $json.sample_rate_hz }},{{ $json.channels }},{{ $json.provider }},{{ $json.model_version }},{{ $json.confidence }},{{ $json.full_text }},{{ JSON.stringify($json.segments) }},{{ $json.diarization }}"
        }
      },
      "id": "store-transcript",
      "name": "Store call + transcript",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [1220, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Everything the judge is allowed to treat as fact about follow-up.\n-- If this returns no rows the worker sends 'unavailable' and Module 4 scores null.\nSELECT coalesce(string_agg(\n  format('  - [%s] %s by %s, %.1fh after this call',\n         i.started_at, i.channel,\n         coalesce(a.full_name, 'unknown'),\n         extract(epoch from (i.started_at - $2::timestamptz))/3600.0),\n  E'\\n' ORDER BY i.started_at), '') AS history\nFROM interactions i\nLEFT JOIN agents a ON a.agent_id = i.agent_id\nWHERE i.customer_phone_e164 = $1\n  AND i.started_at > $2::timestamptz\n  AND i.started_at < $2::timestamptz + interval '14 days';",
        "options": {
          "queryReplacement": "={{ $('Decode PBX filename').item.json.customer_phone_e164 }},{{ $('Decode PBX filename').item.json.started_at }}"
        }
      },
      "id": "followups",
      "name": "Build follow-up history",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [1440, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } },
      "notes": "This is the query that makes Module 4 meaningful for a call. Until Bitrix chats are flowing it returns nothing, and Module 4 correctly scores null instead of a free 100."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.WORKER_URL }}/evaluate",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [{ "name": "X-API-Key", "value": "={{ $env.WORKER_API_KEY }}" }]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ conversation: $('Cohere Arabic ASR').item.json.dialogue, input_type: 'call_transcript', metadata: { asr_confidence: $('Cohere Arabic ASR').item.json.confidence, diarization: $('Cohere Arabic ASR').item.json.diarization, duration_seconds: $('Cohere Arabic ASR').item.json.duration_seconds, channels: $('Cohere Arabic ASR').item.json.channels }, followup_history: ($json.history || 'unavailable'), run_pass1: true, run_pass2: true }) }}",
        "options": { "timeout": 300000 }
      },
      "id": "evaluate",
      "name": "Two AI passes",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1660, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO agent_evaluations (\n  interaction_id, agent_id, schema_version, prompt_version, rubric_version, model,\n  input_type, source_quality, final_score, performance_level, weight_applied,\n  m1_reception, m2_offer, m3_objections, m4_followup, m5_closing,\n  breakdown, evidence, stage_reached, behavior_flags,\n  top_strength, top_weakness, top_recommendation, notes, raw_response\n)\nSELECT $1::uuid, i.agent_id, '1.0', $2, $3, $4,\n       'call_transcript'::input_type, $5::numeric,\n       $6::numeric, $7, $8::numeric,\n       $9::numeric, $10::numeric, $11::numeric, $12::numeric, $13::numeric,\n       $14::jsonb, $15::jsonb, $16::conversation_stage, $17::jsonb,\n       $18, $19, $20, $21, $22::jsonb\nFROM interactions i WHERE i.interaction_id = $1::uuid\nON CONFLICT (interaction_id) DO UPDATE SET\n  final_score = EXCLUDED.final_score,\n  performance_level = EXCLUDED.performance_level,\n  weight_applied = EXCLUDED.weight_applied,\n  raw_response = EXCLUDED.raw_response,\n  updated_at = now();",
        "options": {
          "queryReplacement": "={{ $('Store call + transcript').item.json.interaction_id }},{{ $json.pass2.prompt_version }},{{ $json.rubric_version }},{{ $json.pass2.model }},{{ $('Cohere Arabic ASR').item.json.confidence }},{{ $json.pass2.final_score }},{{ $json.pass2.performance_level }},{{ $json.pass2.weight_applied }},{{ $json.pass2.modules.module1_reception }},{{ $json.pass2.modules.module2_offer }},{{ $json.pass2.modules.module3_objections }},{{ $json.pass2.modules.module4_followup }},{{ $json.pass2.modules.module5_closing }},{{ JSON.stringify($json.pass2.payload.modules) }},{{ JSON.stringify($json.pass2.payload.evidence) }},{{ $json.pass2.payload.stage_reached }},{{ JSON.stringify($json.pass2.payload.behavior_flags) }},{{ $json.pass2.payload.summary.top_strength }},{{ $json.pass2.payload.summary.top_weakness }},{{ $json.pass2.payload.summary.top_recommendation }},{{ $json.pass2.payload.notes }},{{ JSON.stringify($json.pass2.payload) }}"
        }
      },
      "id": "store-eval",
      "name": "Store evaluation",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [1880, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO job_runs (job_name, status, finished_at, items_in, items_ok)\nVALUES ('calls_ingest', 'succeeded', now(), $1::int, $1::int);",
        "options": { "queryReplacement": "={{ $items().length }}" }
      },
      "id": "mark-run",
      "name": "Advance watermark",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [2100, 300],
      "credentials": { "postgres": { "id": "railway-pg", "name": "Railway Postgres" } }
    }
  ],
  "connections": {
    "Every 15 min": { "main": [[{ "node": "Read watermark", "type": "main", "index": 0 }]] },
    "Read watermark": { "main": [[{ "node": "List Drive recordings", "type": "main", "index": 0 }]] },
    "List Drive recordings": { "main": [[{ "node": "Already ingested?", "type": "main", "index": 0 }]] },
    "Already ingested?": { "main": [[{ "node": "New recording?", "type": "main", "index": 0 }]] },
    "New recording?": { "main": [[{ "node": "Decode PBX filename", "type": "main", "index": 0 }], []] },
    "Decode PBX filename": { "main": [[{ "node": "Download audio", "type": "main", "index": 0 }]] },
    "Download audio": { "main": [[{ "node": "Write to shared volume", "type": "main", "index": 0 }]] },
    "Write to shared volume": { "main": [[{ "node": "Cohere Arabic ASR", "type": "main", "index": 0 }]] },
    "Cohere Arabic ASR": { "main": [[{ "node": "Store call + transcript", "type": "main", "index": 0 }]] },
    "Store call + transcript": { "main": [[{ "node": "Build follow-up history", "type": "main", "index": 0 }]] },
    "Build follow-up history": { "main": [[{ "node": "Two AI passes", "type": "main", "index": 0 }]] },
    "Two AI passes": { "main": [[{ "node": "Store evaluation", "type": "main", "index": 0 }]] },
    "Store evaluation": { "main": [[{ "node": "Advance watermark", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1", "saveManualExecutions": true },
  "tags": [{ "name": "customer360" }]
}
